# 项目结构梳理（整理后）

> 项目：VibeHub · AI Agent 实战指南（纯静态 HTML 学习站）
> 整理执行时间：2026-08-12
> 说明：本文档为整理后的**真实最终结构**，可直接作为架构参考。

---

## 一、最终目录结构（真实文件系统）

```
F:\aiagent学习\
├── index.html              # 主站·术语图鉴（307KB，最成熟；已修正指向 courses/ 与 course-notes/）
├── style.css               # 全站共享样式
├── README.md               # 快速上手 / 模块地图（路径已更新）
├── PRODUCT.md              # 产品规格
├── DESIGN.md               # 设计系统（VibeHub 卡片图鉴）
│
├── courses/                # 课程站（原 1/，已改名；含 11 篇 HTML 课 + lesson.css/js/icons.svg）
├── course-notes/           # 原 01-05 课程讲义（已归组）
│   ├── 01-ecosystem/ 02-tools/ 03-workflow/ 04-pitfalls/ 05-comparison/
├── software/               # 软件站（index.html + icons/；安装包已移出项目）
├── demos/                  # 可运行 Python 脚本 + fixtures/
│   ├── agent-file-organizer.py（默认目录已改为 fixtures）
│   ├── demo-agent-loop.py / demo-tool-calling.py
│   └── fixtures/           # 原 test-folder（0 字节占位样本）
├── scripts/                # 工具脚本（原根目录 glm-proxy.py 已归位）
│   └── glm-proxy.py        # GLM API 本地代理（站点"生成"功能支撑）
│
├── .gitignore              # 忽略 .impeccable/ 与 .reasonix/
├── .impeccable/            # 设计工具导出（design.json，不随站发布）
├── .reasonix/              # 编辑器元数据 + 剪贴板附件（不随站发布）
└── 项目结构梳理与整理方案.md  # 本说明
```

### 安装包已移出项目（不在项目文件夹内）
- `F:\AI-Agent-安装包\software_flat\` —— 原 `software/downloads`（约 1.07 GB）
- `F:\AI-Agent-安装包\software_categorized\` —— 原 `software-downloads`（约 1.27 GB，按 10 类归档）
- 共腾出约 **2.3 GB**，项目体积大幅下降。

---

## 二、架构层次

```
┌─ 展示层（三块一体，导航已闭环）──────────────┐
│   术语图鉴  index.html（根）                    │
│   课程站    courses/                           │
│   软件库    software/index.html                │
├─ 内容 / 源层 ────────────────────────────────┤
│   course-notes/  Markdown 讲义（术语图鉴"笔记"区引用）│
│   demos/          可运行脚本                    │
│   scripts/glm-proxy.py  站点"生成"功能支撑      │
├─ 资产层 ─────────────────────────────────────┤
│   style.css 设计令牌 · courses/icons.svg · software/icons/ │
├─ 元 / 工具层（不随站发布）────────────────────┤
│   README / PRODUCT / DESIGN（文档）             │
│   .impeccable/ .reasonix/（工具产物，已 gitignore）│
└───────────────────────────────────────────────┘
```

### 关键关系（已打通）
- 主站 `index.html` 同时引用 `courses/`（课程）与 `course-notes/`（笔记），两者导航互链、主题一致。
- `courses/*.html` 反向引用 `../style.css`、`../index.html`、`../software/index.html`，相对路径保持有效。
- `software/index.html` 仅做软件**索引展示**，不内嵌本地安装包（安装包已外置）。

---

## 三、本次整理执行清单

| 动作 | 状态 |
|------|------|
| `1/` → `courses/`（含 index.html / PRODUCT.md 引用更新） | ✅ |
| `01-05` 归组为 `course-notes/`（含 index.html / README.md 引用更新） | ✅ |
| `glm-proxy.py` 根目录 → `scripts/` | ✅ |
| `demos/test-folder` → `demos/fixtures/`（脚本默认目录同步） | ✅ |
| 删除空垃圾 `链接地址.txt`、临时 `_sizecheck.txt` | ✅ |
| 新增 `.gitignore`（忽略 `.impeccable/`、`.reasonix/`） | ✅ |
| 安装包（约 2.3 GB）移出项目 → `F:\AI-Agent-安装包\` | ✅ |

---

## 四、遗留 / 可选项
- `course-notes/`（Markdown 讲义）与 `courses/`（HTML 课程）目前是**两条平行内容轨道**，建议后续明确二者关系（源与渲染 / 互补），避免内容割裂。
- `software/` 安装包外置后，如需恢复本地下载，可把 `F:\AI-Agent-安装包\` 中的文件放回并更新 `software/index.html` 链接。
- 文档（README/PRODUCT/DESIGN）仍在根目录；若想更整洁可移入 `docs/`（需同步相对路径，可选）。
